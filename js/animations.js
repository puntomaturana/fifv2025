// Inicializar AOS cuando el DOM esté listo
document.addEventListener("DOMContentLoaded", () => {
    AOS.init({
        duration: 800,
        offset: 0,
        once: true,
        mirror: false
    });
});
